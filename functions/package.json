{
  "name": "functions",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.16.0"
  },
  "scripts": {
    "test": "vitest run",
    "deploy:dev": "serverless deploy --stage dev --verbose",
    "deploy:prod": "serverless deploy --stage prod --verbose"
  },
  "devDependencies": {
    "osls": "^4.0.0",
    "vitest": "^4.1.8"
  }
}
